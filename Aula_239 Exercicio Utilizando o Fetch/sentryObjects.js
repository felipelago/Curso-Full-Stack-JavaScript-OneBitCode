export default class SentryObjects {
    constructor(sentryId, name, minAge, maxAge) {
        this.sentryId = sentryId;
        this.name = name;
        this.minAge = minAge;
        this.maxAge = maxAge;
    }
}